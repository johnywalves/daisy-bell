import styled from "styled-components";

export const Main = styled.div`
  display: flex;

  & p {
    font-weight: 700;
    margin-top: 0.75rem;
  }

  & p:first-child {
    margin-right: 1rem;
  }
`;
